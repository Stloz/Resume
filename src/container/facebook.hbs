{{!-- 
Це код container файлу для summary сторінки, 
в якому міститься код, котрий буде віддаватися по шляху (PATH) - "/summary"
--}}

<div>

    <h1>{{name}}</h1>
    <hr>
       
    <ul>
        {{#each users}}

            <h2>Users:</h2>
            <ul>
                <li>id: {{id}}, Name: {{name}}, ⚥ - {{gender}}, {{age}} years.</li>

                <ul>   
                    <h2>Friends:</h2>
                    {{#each friends}}
                        <li>id: {{id}}, Name: {{name}}, ⚥ - {{gender}}, {{age}} years.</li>
                        
                        <ul>
                            <h3>Mutual friends:</h3>
                            {{#each mutual_friends}}
                                <li>id: {{id}}, Name: {{name}},  ⚥ - {{gender}}, {{age}} years.</li>
                            {{/each}}
                        </ul>
                    {{/each}}

                    <h2>Groups:</h2>
                    {{#each groups}}
                        <li>id: {{id}}, Name: {{name}}, Descrption: {{description}}</li>

                        <h3>Members:</h3>
                        {{#each members}}
                            <li>id: {{id}}, Name: {{name}}, ⚥ - {{gender}}, {{age}} years.</li>
                        {{/each}}
                    {{/each}}
                </ul> 
            </ul>
            <hr>
        {{/each}}
    </ul>
</div>

{{!-- 
    Цей код містить лише те, що буде виводитись в {{{body}}} в layout/default.hbs
    Адже наш layout/default.hbs має вже структуру HTML
--}}